{
  "name": "lrc-kit",
  "version": "1.1.1",
  "description": "lrc parser, maker, runner",
  "main": "lib/cjs/lrc-kit.js",
  "module": "lib/esm/lrc-kit.js",
  "keywords": [
    "lrc",
    "lyric"
  ],
  "scripts": {
    "build": "yarn run build:esm && yarn run build:cjs",
    "build:esm": "tsc -p tsconfig.build.json --module es2015 --target es5 --outDir lib/esm",
    "build:cjs": "tsc -p tsconfig.build.json --module commonjs --target es5 --outDir lib/cjs",
    "clean": "rm -rf lib",
    "test:type": "tsc --noEmit",
    "test:lint": "eslint src --ext .ts",
    "test:jest": "jest",
    "test": "npm run test:type && npm run test:lint && npm run test:jest",
    "prepare": "yarn clean && yarn b uild"
  },
  "files": [
    "lib",
    "src"
  ],
  "author": "Weirong Xu <weirongxu.raidou@gmail.com>",
  "homepage": "https://github.com/weirongxu/lrc-kit",
  "repository": {
    "type": "git",
    "url": "https://github.com/weirongxu/lrc-kit.git"
  },
  "bugs": "https://github.com/weirongxu/lrc-kit/issues",
  "license": "MIT",
  "devDependencies": {
    "@raidou/eslint-config-base": "^3.1.3",
    "@types/jest": "^30.0.0",
    "eslint": "^9.39.2",
    "jest": "^30.2.0",
    "prettier": "^3.7.4",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
